[project]
name = "competencies-agent"
version = "0.1.0"
description = "Sidecar chat agent that routes questions to the competencies-db retrieval index."
requires-python = ">=3.13"
dependencies = [
    "gradio>=6.0,<7",
    "openai>=1.40,<3",
    "python-dotenv>=1.0",
]
